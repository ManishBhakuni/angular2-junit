#.travis.yml

language: node_js
node_js:
  - "6.9"
 
branches:
  only:
  - master

env:
  global:
    - CHROME_BIN=/usr/bin/google-chrome
    - DISPLAY=:99.0

before_script:
  #- npm install -g @angular/cli  
  - export CHROME_BIN=chromium-browser
  - export DISPLAY=:99.0
  - sh -e /etc/init.d/xvfb start
  
script:
  #- ng test
  #- ng test --code-coverage 
  #- ng test --code-coveragetrue --watch false && cat ./coverage/*/lcov.info | ./node_modules/coveralls/bin/coveralls.js
  - ng test --code-coveragetrue
  #- ng build
  
deploy:
  provider: azure_web_apps 
  verbose: true

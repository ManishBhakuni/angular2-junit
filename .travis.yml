#.travis.yml

language: node_js
node_js:
  - "6.9"
 
branches:
  only:
  - master

env:
  global:
    - CHROME_BIN=/usr/bin/google-chrome
    - DISPLAY=:99.0

before_script:
  #- npm install -g @angular/cli  
  
script:
  - ng test
  #- ng build
  
deploy:
  provider: azure_web_apps 
  verbose: true
